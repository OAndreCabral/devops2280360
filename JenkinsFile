pipeline{
    agent any
    ...

    stages {
        stage("Stage 1"){
            echo "Pipeline Usando Jenkinsfile"
        }
    }

    stages {
        stage("Stage 2"){
            echo "Pipeline Usando Jenkinsfile"
        }
    }
}

