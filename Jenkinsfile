pipeline{
    agent any

    stages {
        stage("Stage 1"){
            steps{
                echo "Pipeline Usando Jenkinsfile"
            }
        }
    }

    stages {
        stage("Stage 2"){
            steps{
                echo "Pipeline Usando Jenkinsfile"
            }
        }
    }
}

